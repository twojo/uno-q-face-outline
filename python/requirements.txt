# Runtime dependencies for on-device face detection via Qualcomm AI Hub.
# These are OPTIONAL — the app works without them (browser-only mode).
# Install on the Uno Q only; not needed on the dev machine for the
# compile step (that uses qai-hub and qai_hub_models instead).

# TFLite inference engine (pick ONE):
#   tflite-runtime          # Lightweight, recommended for QRB2210
#   tensorflow              # Full TF (much larger, not recommended)

# Required by face_detector_mpu.py:
#   numpy                   # Array operations for model I/O
#   opencv-python-headless  # Camera capture + image preprocessing

# To install on the Uno Q:
#   pip install tflite-runtime numpy opencv-python-headless

# For the AI Hub compile helper (dev machine only, not Uno Q):
#   pip install qai-hub qai_hub_models torch
