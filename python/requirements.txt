# Runtime dependencies for Wojo's Uno Q Face Outline Demo
#
# Core dependencies (required on the Uno Q):
#   These are provided by the Arduino App Lab SDK and do not need
#   separate installation — they are part of the Bricks runtime.
#   Listed here for documentation only:
#     arduino.app_utils (Logger, App, Bridge)
#     arduino.app_bricks.web_ui (WebUI)

# Optional: On-device face detection via Qualcomm AI Hub
# Install on the Uno Q only if you want MPU-side inference.
# The demo works without them (browser-only mode).
tflite-runtime
numpy
opencv-python-headless

# For the AI Hub compile helper (dev machine only, not Uno Q):
#   pip install qai-hub qai_hub_models torch
